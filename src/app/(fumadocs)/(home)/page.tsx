// Placeholder only: the postbuild script overwrites out/index.html with
// page-source/home.html verbatim, since that page needs its own raw
// <html>/<head>/<script> document rather than the shared fumadocs layout.
export default function HomePage() {
  return null;
}
